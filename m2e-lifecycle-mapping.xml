<?xml version="1.0" encoding="UTF-8"?>
<lifecycleMappingMetadata>
  <pluginExecutions>
    <!--
      JDTLS/m2e import-only override:
      Skip resources mojo execution to avoid extension/context failures during IDE import.
      Build behavior in CLI Maven remains unchanged.
    -->
    <pluginExecution>
      <pluginExecutionFilter>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-resources-plugin</artifactId>
        <versionRange>[2.4,)</versionRange>
        <goals>
          <goal>resources</goal>
          <goal>testResources</goal>
          <goal>copy-resources</goal>
        </goals>
      </pluginExecutionFilter>
      <action>
        <ignore />
      </action>
    </pluginExecution>

    <!--
      Project-specific import override:
      Avoid executing antrun "copy-config" (process-resources) inside m2e/JDTLS.
      This execution depends on extension-scoped MavenProject context that is not
      available during IDE import in some environments.
    -->
    <pluginExecution>
      <pluginExecutionFilter>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-antrun-plugin</artifactId>
        <versionRange>[3.1.0,)</versionRange>
        <goals>
          <goal>run</goal>
        </goals>
      </pluginExecutionFilter>
      <action>
        <ignore />
      </action>
    </pluginExecution>

    <!--
      OpenAPI sources are part of the compile model for this project.
      Allow m2e/JDTLS to execute this goal during import so generated packages
      (e.g. fr.bdf.*) are attached to the Java classpath.
    -->
    <pluginExecution>
      <pluginExecutionFilter>
        <groupId>org.openapitools</groupId>
        <artifactId>openapi-generator-maven-plugin</artifactId>
        <versionRange>[0,)</versionRange>
        <goals>
          <goal>generate</goal>
        </goals>
      </pluginExecutionFilter>
      <action>
        <execute />
      </action>
    </pluginExecution>

  </pluginExecutions>
</lifecycleMappingMetadata>
